---
title: "Rubric: Survey Plan"
subtitle: "EMSE 6035: Marketing Analytics for Design Decisions, Fall 2022"
toc: false
---

```{r setup, include=FALSE, warning=FALSE, message=FALSE}
# Load options and libraries
library(tidyverse)
library(knitr)
library(kableExtra)
knitr::opts_chunk$set(
    comment = "#>",
    warning = FALSE,
    message = FALSE,
    fig.path = "figs/"
)
```

### `r sum(rubric$maxPoints)` Total Points

```{r, echo=FALSE}
rubric <- read_csv("rubric.csv") %>%
  mutate(description = paste0("<b>", points, '</b><br>', description)) %>%
  select(-points) %>%
  spread(key = rating, value = description) %>%
  select(-category) %>%
  rename(Category = label) %>%
  arrange(order) %>%
  select(-order) %>%
  select(-maxPoints) %>%
  kable(format = 'html', escape = FALSE) %>%
  kable_styling(bootstrap_options = "striped")
```
